# CivicLenZ — Immediate Deep-Dossier Swarm Activation Directive — 2026-09-17

## 1. Objective

Turn the physically proven CivicLenZ/HERMES infrastructure into the intended production data-acquisition machine now.

This directive is execution-focused. It does not authorize another broad architecture rediscovery pass.

The current short-term objective is:

1. preserve and finish the two existing staged producer returns;
2. preserve all current canonical truth/evidence/attempt history;
3. activate the first full deep-dossier swarm for the already identified current Florida Governor canonical subject;
4. keep discovery moving to additional officials while that dossier deepens;
5. activate GIS/boundary/election linkage in parallel;
6. convert completed dynamic scopes into persistent monitoring obligations;
7. prove promise/commitment discovery and later-evidence monitoring;
8. move from manual cohort handling to resource-governed continuous work as soon as each capability path is physically proven.

## 2. Do not discard current recovery work

The producer staged-return recovery effort remains valid and must be completed.

Do not abandon or recreate the two existing durable staged returns merely because the strategic focus is now deep-dossier production.

Preserve:
- canonical attempts;
- producer executions;
- staged result packages;
- authorization history;
- receipt/validation history;
- current monitoring/Academy records.

Complete their current physical lineage first or in parallel with independent unblocked canonical work.

## 3. Immediate priority shift

The Florida DOS candidate-filings path remains a supported production capability and should continue as part of the election/candidate lane.

It is no longer the sole or primary definition of activation success.

The next controlling success condition is physical proof of the full subject-level swarm:

`canonical subject -> applicable ResearchContract graph -> many independent durable jobs -> parallel worker execution -> evidence -> validation -> gaps -> monitoring`

## 4. First reference subject

Use the existing canonical current Florida Governor subject as the first `STATE_GOVERNOR` deep-dossier reference subject.

Do not create a duplicate Person, Seat or Occupancy.

Fetch the exact canonical identifiers from current state before mutation.

This subject is a reference proof, not a reason to stop discovery of other subjects.

## 5. Build/verify the subject fan-out planner

Inspect whether canonical HERMES already physically implements full applicable ResearchContract fan-out after identity resolution.

Required behavior:
- enumerate all applicable Core/STATE_GOVERNOR/Dynamic scopes;
- inspect current evidence/currentness for each;
- create/reconcile missing/stale/conflicting durable ResearchWorkIdentities;
- encode only genuine dependency edges;
- avoid duplicate work identities;
- prioritize current/election/accountability work appropriately;
- create monitoring obligations after bounded reconciliation.

If this behavior already exists, connect/activate it rather than creating another planner.

If incomplete, implement the smallest generalized repair within canonical HERMES.

Do not hard-code the Florida Governor as a special research workflow; the same mechanism must work for future subjects/office classes.

## 6. Full STATE_GOVERNOR contract fan-out

Generate/reconcile the full applicable research graph, including at minimum where implemented/appropriate:

### Foundation
- identity resolution and aliases;
- Occupancy/Seat relationship;
- official and campaign sites;
- portraits/media assets;
- official contact channels;
- official/campaign social accounts.

### Background/history
- biography;
- education;
- career;
- military/public service history where applicable;
- political history;
- prior offices;
- election/campaign history.

### Government activity
- executive orders;
- executive actions;
- appointments;
- vetoes;
- bill signings;
- budget actions;
- other governor-specific powers/actions;
- relevant boards/commissions/official responsibilities where supported.

### Campaign/accountability
- campaign platforms;
- campaign promises;
- explicit public commitments and non-commitments;
- public statements;
- debates/speeches/interviews/podcasts;
- promise evidence relationship monitoring.

### Finance/disclosure
- campaign committees;
- contributions/expenditures;
- finance reconciliation;
- financial disclosures;
- assets/liabilities/income/business interests where lawfully public and relevant.

### Public record/ethics
- ethics records;
- investigations and dispositions;
- materially relevant public court records;
- conflicts/relationship evidence without unsupported motive inference.

### Relationships
- staff/appointment relationships;
- donor/campaign committee relationships;
- organization/business relationships;
- other public civic relationship edges with evidence.

### Media/social/news
- official press releases;
- material news chronology;
- interviews/debates/podcasts;
- official/campaign social feeds;
- materially relevant social posts;
- video/audio transcript/timecode evidence where available.

### Territory/elections/GIS
- Florida statewide Seat/territory linkage;
- authoritative boundary/version where applicable;
- public civic locations;
- election/cycle/CandidateCampaign linkage;
- map/address-resolution readiness checks.

### Monitoring/currentness
- monitoring obligation for every dynamic source/scope;
- source health/schema fingerprints;
- next_check/freshness rules;
- change-event generation.

Unsupported capabilities stay explicitly blocked. They do not block unrelated scopes.

## 7. Asynchronous worker execution rule

Do not execute the dossier serially.

Once jobs are eligible, fill available worker capacity across independent scopes.

A worker that completes one bounded task must terminalize it and immediately lease the next eligible work item.

It must not wait for sibling tasks on the same subject.

Examples:
- portrait worker finishes Governor -> moves to next eligible subject;
- promise worker finishes one bounded promise-discovery scope -> moves to next eligible promise/subject work;
- finance worker finishes one filing-period reconciliation -> moves to next finance work;
- GIS worker completes one boundary artifact -> moves to next geography;
- validation worker validates one package -> moves to next validation job.

Prime remains the orchestrator and dependency authority.

## 8. Discovery lane must continue

While the reference Governor swarm is running, continue Seat/officeholder/candidate discovery for the next eligible Florida officials.

When a new identity reaches safe canonical resolution:
- generate that subject's applicable contract graph immediately;
- place work into the same global capability pools;
- do not wait for the Governor dossier to finish.

This creates an overlapping conveyor:

`Discovery N+1` while `Identity N` while `Research N-1` while `Validation N-2` while `Monitoring N-3`.

## 9. Dynamic concurrency

Do not set swarm size from a fixed aspirational number.

Use the current Resource Governor or implement/connect the smallest required mechanism so Prime fills as much capacity as is physically safe.

Observe:
- VPS CPU/RAM/I/O/network;
- database connections/latency;
- R2/Cloudflare health;
- browser session capacity;
- source 429/error rates;
- queue depth;
- lease expirations;
- validation backpressure;
- model/API quotas/cost.

Increase concurrency when measured headroom exists and useful eligible backlog exists. Reduce it when saturation/source throttling/failure spikes occur.

Preserve interactive/recovery headroom.

## 10. Promise intelligence as first-class proof

For the reference subject, physically prove a campaign-promise/public-commitment pipeline.

Required stages:
1. discover attributable campaign/public commitment sources;
2. retrieve/preserve the underlying source;
3. extract material commitment with context/qualifiers/date;
4. normalize into a canonical commitment/proposition candidate;
5. independently validate identity/source/context;
6. reconcile repeated formulations without losing source evidence;
7. create a persistent monitoring obligation;
8. test later-evidence matching against at least one real source update when available, without manufacturing a contradiction.

Do not fabricate a promise-conflict event solely to make acceptance pass.

Potential conflicts remain evidence relationships requiring validation. Do not score/rank the official.

## 11. Social/media monitoring proof

For identified official/campaign accounts and source feeds:
- register the authoritative/attributable account/source with evidence;
- preserve source identifiers/URLs;
- establish event/feed/polling monitoring as appropriate;
- retrieve material new content;
- preserve timestamps/locators/media identifiers;
- create research/validation work for materially relevant content;
- avoid indiscriminate private-life surveillance.

Workers that discover accounts move on; durable monitoring obligations remain behind.

## 12. GIS/map proof

In parallel with the reference dossier, prove the geospatial lane for the subject's Seat/geography using existing map architecture.

Required physical outputs include applicable:
- canonical Seat geography link;
- authoritative/versioned boundary artifact or truthful unresolved state;
- SeatBoundaryAssignment;
- public civic location(s) where appropriate;
- election linkage;
- address-resolution test/readiness state;
- evidence/version/currentness metadata.

Do not imply private residence locations.

## 13. Gap Detector recursion

After each bounded scope result/validation, run or trigger canonical gap evaluation.

New discoveries must recursively create/reconcile follow-up work where applicable.

Examples:
- newly found prior office -> historical election/vote/finance scopes;
- newly found campaign committee -> finance reconciliation;
- newly found organization -> relationship work;
- newly found social account -> historical retrieval + monitor;
- new promise -> promise timeline + monitor;
- new source family -> Source Registry/adapter evolution.

Physically prove at least one real recursive follow-up generated from new evidence if such evidence appears; do not synthesize it for acceptance.

## 14. Monitoring conversion

When a dynamic scope reaches a reconciled bounded state, do not leave it inert.

Create/activate the corresponding monitoring obligation with:
- subject/scope/source;
- current_as_of;
- next_check;
- cadence/event rule;
- previous fingerprint/cursor where applicable;
- health/failure state;
- next-action semantics.

Physically prove that due monitoring can create/reopen research work when warranted.

## 15. Validation and publication

Every material result continues through canonical validation/reconciliation.

Workers/producers remain `extracted_unreviewed` by default and cannot self-promote material civic truth.

Public projection remains limited to publication-eligible validated canonical state.

No political ranking, candidate recommendation, official score, ideological grade or voting recommendation is permitted.

Promise/accountability output describes evidence relationships with visible sources and currentness.

## 16. Immediate implementation order

After current staged-return recovery is safely resumed, execute in this order while allowing independent tracks to overlap:

### A. Revalidate current canonical/prod heads and preserve newer work.
### B. Verify/repair full subject fan-out planner.
### C. Load exact existing Florida Governor canonical identifiers.
### D. Generate/reconcile full `STATE_GOVERNOR` ResearchContract graph.
### E. Dispatch independent supported scopes up to Resource Governor capacity.
### F. Keep discovery/identity lane moving to next officials.
### G. Prove evidence -> validation -> gap recursion on the reference swarm.
### H. Prove persistent monitoring conversion for dynamic scopes.
### I. Prove promise discovery + monitoring pipeline.
### J. Prove GIS/boundary/election linkage in parallel.
### K. Extend the same fan-out mechanism to each newly identified official.
### L. Replace any remaining manual per-job rotation with bounded Prime-owned automatic scheduling once the affected path is physically proven.

Do not wait for all of B-L to run strictly serially when dependencies allow parallel execution.

## 17. What not to optimize for

Do not optimize for:
- passing arbitrary batch sizes as the end product;
- maximizing raw job count without useful contract coverage;
- shallow profile counts;
- synthetic completion percentages;
- worker registration without execution;
- one giant subject-specific agent;
- one agent waiting for the whole dossier;
- one failing capability freezing the entire subject/system.

Optimize for:
- applicable deep-scope coverage;
- evidence quality and traceability;
- physically validated throughput;
- independent parallel work;
- recursive graph expansion;
- monitoring conversion;
- per-capability reuse across subjects;
- safe maximum utilization.

## 18. Reference swarm acceptance

The first STATE_GOVERNOR deep-dossier swarm passes its production proof when physical evidence shows:

1. the existing canonical subject was reused without duplicate identity;
2. full applicable ResearchContract fan-out was generated/reconciled;
3. multiple independent research domains ran concurrently;
4. no sibling-wait barrier serialized the dossier;
5. workers completed work and moved to other eligible work;
6. discovery continued for additional subjects during enrichment;
7. real source retrievals/evidence objects were produced;
8. independent validation/reconciliation occurred;
9. Gap Detector re-evaluated subject coverage and generated truthful next work where warranted;
10. dynamic scopes created durable monitoring obligations;
11. promise/commitment research produced evidence-backed records where sources support them;
12. promise monitoring is configured to detect future relevant evidence;
13. GIS/boundary/election/map obligations are active with evidence-backed status;
14. Resource Governor controlled concurrency from measured physical capacity;
15. unsupported scopes remained blocked rather than fabricated;
16. no worker/producer gained canonical verification/publication authority;
17. the machine continued after the interactive engineering session disconnected.

## 19. Handoff reporting format

Future agents must report separately:
- canonical repo/head;
- deployed Prime release;
- producer repo/head/revision(s);
- reference subject canonical IDs;
- ResearchContracts applicable/generated;
- jobs eligible/running/blocked by capability;
- actual measured concurrency;
- completed worker runs by domain;
- evidence/validation/gap counts;
- monitors created/due/healthy;
- promise records/monitoring state;
- GIS/map readiness state;
- newly discovered subjects and their fan-out state;
- exact demonstrated blockers;
- next automatic work already scheduled.

Do not report one global completion percentage or stop merely because a checkpoint was reached.
