# Miami-Dade County directory workstream

This directory belongs exclusively to the `fl-miami-dade-county-directory` workstream declared in `data/operations/florida-work-allocation.json`.

Officeholder extracts are written only to `data/staging/florida/local/miami-dade/` as `extracted_unreviewed` records. They are not public profiles.
