import { WorkspaceExperience } from '@/components/workspace-experience';

export const metadata = { title: 'Promise Tracker' };

export default function PromiseTrackerPage() {
  return <WorkspaceExperience page="promises" />;
}
