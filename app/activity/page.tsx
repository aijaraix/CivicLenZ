import { WorkspaceExperience } from '@/components/workspace-experience';

export const metadata = { title: 'My Activity' };

export default function ActivityPage() {
  return <WorkspaceExperience page="activity" />;
}
