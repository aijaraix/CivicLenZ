import { WorkspaceExperience } from '@/components/workspace-experience';

export const metadata = { title: 'Settings' };

export default function SettingsPage() {
  return <WorkspaceExperience page="settings" />;
}
